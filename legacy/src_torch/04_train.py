"""Archived legacy script.

Original path:
  - src/04_train.py

This was part of the earlier (pre-Plan vNext) torch-based GNN/LSTM fusion model.
Use the Plan vNext pipeline in `src/` instead.

The original file is large and isn’t needed for the current project path, so it’s
referenced here for archival only.
"""
